function calculator(operation, num1, num2) {
    if(operation === "add"){
        function add() {
            return num1 + num2;
        }
    }

    if (operation === "subtract") {
        function subtract(param) {
            return num1 - num2;
        }
    }

    
}